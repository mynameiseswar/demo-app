import { MathOperationsPipe } from './math-operations.pipe';

describe('MathOperationsPipe', () => {
  it('create an instance', () => {
    const pipe = new MathOperationsPipe();
    expect(pipe).toBeTruthy();
  });
});
